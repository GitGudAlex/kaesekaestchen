package dreamTeam;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import java.util.ArrayList;


public class GUITest {

  /*  MatchfieldSettings matchfield = new MatchfieldSettings(3,1,false,false, false);
    FieldFactory factory = new FieldFactory();
    ArrayList<IField> fieldList = new ArrayList<>();

    @Before
    public void fillFieldList(){
        fieldList.add(0, factory.generateField(0,0,0,"normal"));
    }

    @Test
    public void pointsToAdd() {
        Assert.assertEquals(0, GUI.PointsToAdd(0, matchfield));
    }*/
}